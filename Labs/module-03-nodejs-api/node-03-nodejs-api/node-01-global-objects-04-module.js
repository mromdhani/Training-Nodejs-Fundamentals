/* This global object is a reference to the current module. 
   It contains information about the current module that CommonJs 
   can use to figure out how to import the module.

   Every time you create a file just like we did when we created main.js, it becomes a module.
*/

console.log(module);